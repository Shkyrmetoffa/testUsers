export type User = {
    name: string;
    phone: number;
    id:           number;
}
export class Posts {
    posts: string;
    name: string;
}
export type Comments = {
   comments: string;
}